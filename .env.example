# Example environment variables (replace placeholders with real values)
# DATABASE_URL format: postgresql://<username>:<password>@<host>:<port>/<database>
DATABASE_URL=postgresql://username:password@localhost:5432/urlshortener

# Base URL of your app
BASE_URL=http://localhost:8000
